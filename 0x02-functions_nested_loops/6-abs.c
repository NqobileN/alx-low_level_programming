#include "main.h"

/**
 * _abs - computes the absolute value of an interger
 * @n: n is an interger
 * Return: Always success
 */

int _abs(int n)
{
	if (n > 0)
	{
		return (0);
	}
	else if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
